# Environment / secrets
# NOTE: .env is intentionally NOT ignored — this project commits it on purpose
# (real DB connection string lives there so Docker/local dev work with zero setup).
.env.local
.env.*.local

# Dependencies
node_modules/
server/node_modules/
client/node_modules/

# Build output
client/dist/
server/dist/

# Logs
*.log
npm-debug.log*

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Docker volumes (local)
pgdata/
